import { TransformationModel } from './transformation-model';

describe('TransformationModel', () => {
  it('should create an instance', () => {
    expect(new TransformationModel()).toBeTruthy();
  });
});
